#include <tunables/global>

/usr/bin/mongod {
  # Include all basic rules and system profiles
  #include <abstractions/base>
  #include <abstractions/nameservice>
  #include <abstractions/user-tmp>
  #include <abstractions/ssl_certs>

  # Allow MongoDB to read and write its data
  /mnt/data/mongo/** rwk,

  # Deny access to specific system files and directories for security
  deny /sys/[^f]*/** rwklx,
  deny /proc/** rwklx,
  deny /root/ r,
  deny /root/** r,
  deny /etc/shadow r,
  deny /etc/shadow- r,

  # Allow necessary network operations
  network inet stream,
  network inet dgram,
  network inet6 stream,
  network inet6 dgram,

  # Allow MongoDB to execute and interact with itself
  /usr/bin/mongod mr,
  /usr/bin/mongo mr,
  /usr/bin/mongos mr,

  # Miscellaneous necessary permissions
  capability dac_override,
  capability dac_read_search,
  capability net_bind_service,
  capability setgid,
  capability setuid,
  capability sys_chroot,
  capability sys_resource,

  # Allow MongoDB to use shared memory
  /dev/shm/** rwk,
  
  # Logs
  /var/log/mongodb/** rw,

  # Deny other dangerous capabilities
  deny capability sys_module,
  deny capability sys_rawio,
  deny capability sys_ptrace,
  deny capability sys_admin,
  deny capability sys_boot,
  deny capability sys_nice,
  deny capability sys_pacct,
  deny capability sys_tty_config,
  deny capability mknod,
  deny capability audit_write,
  deny capability audit_control,
  deny capability mac_override,
  deny capability mac_admin,
}
